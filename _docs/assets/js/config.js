
// Set base URL for redirects
const BASE_URL = window.location.origin; // For root level deployments

// Or if your app is in a subdirectory:
// const BASE_URL = window.location.origin + '/tickbug';

